Логін це юзкейс? Це точно не просто ентіті, бо ми робимо в ньому запити. Register теж.

Sidebar це віджет або частина сторінки?

Дашборд це сторінка, яка має різні роути. За-замовчення у нас обрано Overview, це Ecomerce скрін.

Для створення цієї сторінки нам потрібно кілька полів та кнопок із Shared, які ми зберемо у форму в сегменті ui цієї сторінки. Потім, у сегменті api ми визначимо змінний запит, щоб створити статтю на бекенді.

Щоб перевірити запит перед відправкою, нам потрібна схема валідації, і добрим місцем для неї є сегмент model, оскільки це модель даних. Там ми згенеруємо повідомлення про помилку, а відобразимо його за допомогою ще одного компонента в сегменті ui.

BHuser@gmail.com
12345678
